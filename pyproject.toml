[project]
name = "state-parser"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Chad Lowe", email = "pfmsoft.dev@gmail.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
state-parser = "state_parser:main"

[build-system]
requires = ["uv_build>=0.11.21,<0.12.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.0.3",
    "ruff>=0.15.17",
]
